/** Shared validation bounds for guided diagnostic intake (admin UI + API + persistence). */
export const DIAGNOSTIC_MAX_ROUNDS_MIN = 1;
export const DIAGNOSTIC_MAX_ROUNDS_MAX = 20;
export const DIAGNOSTIC_QUESTIONS_PER_ROUND_MIN = 2;
export const DIAGNOSTIC_QUESTIONS_PER_ROUND_MAX = 8;
/** Tap options shown for each multiple-choice question (schema uses exactly this count per question). */
export const DIAGNOSTIC_OPTIONS_PER_QUESTION_MIN = 2;
export const DIAGNOSTIC_OPTIONS_PER_QUESTION_MAX = 8;
